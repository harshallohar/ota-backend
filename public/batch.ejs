<%- include('includes/header.ejs') %>
<main>
  <% if (batchList.length === 0) {%>
  <p>Nothing to show</p>
  <%} else {%>
  <table>
    <tr>
      <td>Sl No</td>
      <td>Name</td>
      <td>Added Date</td>
    </tr>
    <% batchList.forEach((val, ind) => { %>
    <tr>
      <td> <%= ind + 1 %> </td>
      <td><a href="/batches/<%= val.id %>"><%= val.name %></a></td>
      <td><%= val.batchDate.toLocaleString('in') %></td>
    </tr>
    <%})%>
  </table>
  <%}%>
</main>
<%- include('includes/footer.ejs') %>
